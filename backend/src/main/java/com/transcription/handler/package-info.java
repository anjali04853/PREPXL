/**
 * WebSocket handlers for managing client connections and message routing.
 */
/**
 * WebSocket handlers for real-time audio streaming and transcription.
 * Contains handlers for managing WebSocket connections and routing audio data.
 */
package com.transcription.handler;
